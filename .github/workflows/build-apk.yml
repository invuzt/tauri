name: Auto Create and Build Tauri APK (Super Fast & Tiny)
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android # Fokus ke arsitektur HP modern agar build 4x lebih cepat

      # Fitur Cache agar build kedua dan seterusnya berjalan kilat
      - name: Cache Rust Dependencies
        uses: swatinem/rust-cache@v2

      - name: Cache Node Modules
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      # Inisialisasi otomatis dan injeksi script Tauri ke package.json
      - name: Initialize Tauri Project Silently
        run: |
          npm init -y
          sed -i 's/"test": "echo \\"Error: no test specified\\" \&\& exit 1"/"tauri": "tauri"/g' package.json
          npm install @tauri-apps/cli@latest @tauri-apps/api@latest
          npx tauri init --app-name "Aplikasi Arsip" --window-title "Arsip Internal" --frontend-dist "../src" --dev-url "http://localhost:1420" --before-dev-command "" --before-build-command ""
          
          # Mengubah bundle identifier agar unik demi Android
          sed -i 's/"com.tauri.dev"/"com.internal.arsipku"/g' src-tauri/tauri.conf.json
          
          # Optimasi Ukuran APK Semut (LTO & Strip)
          echo -e "\n[profile.release]\nlto = true\ncodegen-units = 1\nstrip = true\npanic = 'abort'" >> src-tauri/Cargo.toml

      # Menginisialisasi komponen Android native
      - name: Initialize Android Platform
        run: npx tauri android init

      # Proses kompilasi menjadi APK Unsigned
      - name: Build APK
        run: npx tauri android build

      # Perbaikan Utama: Mengunggah file APK dengan package upload-artifact yang benar
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: tauri-android-apk
          path: src-tauri/gen/android/app/build/outputs/apk/release/**/*.apk